void CH4(double C, double H) {
    const C1 = C;
    const H4 = H * H + H * H;
}
void C5H12(double C, double H) {
    const C5 = C * C * 2 + C;
    const H12 = ( H * H + H * H + H * H ) * 2;
}
void C17H36(double C, double H) {
    const C17 = (C * C) * 4 + C;
    const H36 = ( H * H + H * H + H * H ) * 6;
}
